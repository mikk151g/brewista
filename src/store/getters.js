export default {
    latestBrews(state) {
        return state.latestBrews;
    },
    coffeeTypes(state) {
        return state.coffeeTypes;
    },
    favorites(state) {
        return state.favorites;
    },
    newRecipies(state) {
        return state.newRecipies;
    }
};